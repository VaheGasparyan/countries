/// My Components
import Header from 'components/header';

/// MUI
import {Box} from "@mui/material";

const HomePage = () => {
    return (
        <Box className='home'>
            <Header />
        </Box>
    );
};

export default HomePage;